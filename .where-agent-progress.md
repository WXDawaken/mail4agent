# Current Progress

Last updated: `2026-03-29`

## Current Mainline

- The private repo `WXDawaken/mail4agent` is now cloned locally at `E:\agent_misc\mail4agent`.
- This workspace is now being used as the canonical Python dogfood baseline for local mailbox server and CLI validation.
- The current local checkout is on `codex/mail4agent-dogfood-wave1`.
- The first benchmark-oriented Rust migration scenario set is now documented in `docs\rust-port-test-scenarios-20260322.md`.
- A second benchmark-oriented planning note now documents feature-expansion requirements that build on the existing Python mailbox product surface in `docs\feature-extension-test-requirements-20260322.md`.
- The current working language decision is now `Python-first, Rust-selective`, with Python staying the product source of truth and Rust advancing by bounded backend capability.
- The canonical repo now includes the first Wave 1 Python dogfood runtime features: admin direct mailbox access, retry queue visibility, and mailbox-scoped thread summary/unread state.
- The canonical repo now also includes `small_session_logout_and_expiry_introspection`: `POST /logout`, CLI `logout`, and `GET /whoami` session expiry metadata (`created_at`, `expires_at`, `expires_in_seconds`, `last_used_at`).
- A repo-local dogfood smoke path now exists via `dogfood_smoke_bootstrap.py`, `launch_dogfood_agent.ps1`, and `docs\dogfood-medium-smoke-20260324.md`, and its core bootstrap/operator/planner flow has now been smoke-validated locally.
- That same dogfood path has now also been exercised with real medium Codex planner/reviewer runs plus one follow-up planner revision pass on the same thread.
- The same dogfood runtime now also emits an operator mailbox profile and supports a bounded code-editing operator lane via `docs\dogfood-operator-update-flow-20260324.md`; planner/reviewer stay `medium`, while operator defaults to `high`.
- That new operator lane is now smoke-validated against a temporary local server on `127.0.0.1:8792`: bootstrap wrote the operator profile, harness-token login created a group-mailbox operator session, and `launch_dogfood_agent.ps1 operator` completed a real claim/thread/reply/ack cycle on one no-op operator delivery.
- A follow-on maintenance drill note now exists at `docs\dogfood-feedback-server-update-scenario-20260324.md`; it defines the next dogfood test as a bounded feedback-driven server update with restart notice, harness-token re-login, and post-update thread continuity checks.
- That maintenance note now also names the best-aligned existing small feature direction: `small_session_logout_and_expiry_introspection`, because it fits restart-and-relogin validation better than `small_inbox_list_and_filters`, while `small_retry_queue_visibility` is already part of the canonical baseline.
- A concrete maintenance drill playbook now also exists at `docs\dogfood-feedback-server-update-drill-20260324.md`; it includes operator kickoff commands, reviewer feedback payload shape, maintenance notice commands, re-login checks, and post-update verification steps.
- That maintenance drill now also has a repo-local automation entrypoint at `dogfood_feedback_update_drill.py`.
- The first mailbox-native maintenance drill is now complete and documented in `docs\dogfood-feedback-server-update-report-20260324.md`.
- The next concrete maintenance target is now fixed in `docs\dogfood-feedback-server-update-thread-defaults-20260324.md`: add a session-scoped `default_inbox_address` so planner/reviewer thread-state commands stop needing explicit `--to-address` recovery.
- That session-scoped thread-default target is now also promoted into the canonical baseline: agent-session `whoami` exposes `default_inbox_address`, and thread-state routes plus CLI inference now use it so logged-in planner/reviewer flows no longer need explicit `--to-address` recovery in the common case.
- The follow-on maintenance drill is now also complete and documented in `docs\dogfood-feedback-server-update-thread-defaults-report-20260324.md`; it verifies that session-token-only `whoami`, `thread-summaries`, and post-restart `mark-thread-read` work without mailbox config or explicit `--to-address`.
- The canonical repo now also includes a first minimal inbox read surface: `GET /inbox`, `client.py inbox`, exact `message_type` filtering, and agent-session `default_inbox_address` inference when `--to-address` is omitted.
- That inbox read surface now also supports unread-only filtering: `GET /inbox?unread_only=true` and `client.py inbox --unread-only` both respect mailbox thread-read state while keeping session default inbox inference.
- That inbox read surface now also supports exact thread filtering: `GET /inbox?thread_id=<THREAD_ID>` and `client.py inbox --thread-id <THREAD_ID>` both preserve the same session default inbox inference.
- The repo now also has a shared `mailbox_worker.py` module: `client.py consume` now uses it as the generic claim/heartbeat/ack/nack primitive instead of keeping the full worker loop inline.
- A first role-aware oncall path now lives in `mailbox_oncall.py` plus `launch_dogfood_oncall_agent.ps1`; the initial scope is operator-only and reuses the existing `.tmp_dogfood` runtime assets plus a claimed-delivery prompt.
- That operator oncall path is now also smoke-validated against a temporary local server: the supervisor claimed one operator delivery, launched a fresh `high` Codex operator run, the child replied exactly once through mailbox, and the supervisor acked the claimed delivery with retry queue left empty.
- The current oncall path is still stateless across supervisor processes, but claims now default to mailbox-thread serialization. Two supervisors will not hold the same delivery at once, and they also will not simultaneously claim different active deliveries from the same thread within one mailbox route.
- A first cross-project mailbox dogfood scenario now lives in `docs\dogfood-cross-project-supply-demand-scenario-20260326.md`; it models `consumer_app -> shared_tools` as a bounded supply-demand relationship with explicit cross-project routing, supplier-side oncall, and mailbox-native request / clarification / delivery / acceptance threads.
- That scenario now also has an executable first drill at `docs\dogfood-cross-project-routing-explain-drill-20260326.md` plus a dedicated bootstrap helper `dogfood_cross_project_bootstrap.py`.
- The generic dogfood launchers are now config-driven instead of hardcoding `project_id = mail4agent`, so the same planner/operator launcher path can be reused against cross-project runtimes.
- The first live cross-project routing-explain drill is now complete and documented in `docs\dogfood-cross-project-routing-explain-report-20260326.md`; `consumer_app` planner sent one `integration_request` to `shared_tools` intake, supplier-side operator oncall replied on the same mailbox thread, the consumer sent `integration_acceptance`, and the final retry queue remained empty.
- The canonical repo now also supports optional inbox sender filtering: `GET /inbox?from_address=<ADDRESS>` and `client.py inbox --from-address <ADDRESS>` preserve the same session default inbox inference while narrowing results to one sender.
- The second live cross-project drill is now also complete and documented in `docs\dogfood-cross-project-bounded-patch-request-report-20260326.md`; `consumer_app` planner sent one bounded patch request to `shared_tools` intake, supplier-side operator oncall implemented the inbox sender filter, local follow-up validation passed, and the consumer sent `integration_acceptance` on the same mailbox thread.
- The canonical repo now also supports mailbox-native consumer-side handoff: `client.py handoff` forwards one visible source message to a different mailbox while preserving `thread_id` and embedding a source snapshot so cross-mailbox review can continue without widening thread visibility.
- The third live cross-project drill is now also complete and documented in `docs\dogfood-cross-project-handoff-report-20260326.md`; planner forwarded a supplier reply to integrator via mailbox handoff, integrator reviewed the handoff payload, and integrator sent acceptance back on the same thread.
- A new architecture note now also exists at `docs\mailbox-oncall-separation-20260328.md`; it records the preferred split of `mailbox server` as the fact/routing layer, `oncall server` as the supervision layer, and app-server-managed agents as execution backends rather than durable workflow state.
- A concrete follow-on implementation note now also exists at `docs\mailbox-oncall-implementation-plan-20260328.md`; it recommends first extracting `oncall_supervisor.py` and `oncall_registry.py` from the current oncall path before adding any sticky per-thread agent behavior.
- That first oncall refactor step is now implemented: `mailbox_oncall.py` is reduced to CLI/config assembly, `oncall_supervisor.py` owns delivery-loop bookkeeping, and `oncall_registry.py` persists `.oncall/<role>-last-run.json` plus `.oncall/registry/<role>.json` for bounded recoverable supervision metadata.
- The next oncall refactor step is now also implemented: `oncall_exec_codex.py` owns the current Codex CLI launcher/backend contract, `oncall_supervisor.py` now consumes structured execution results instead of raw subprocess wiring, and `mailbox_oncall.py` no longer knows the PowerShell launch details directly.
- The next observability step is now also implemented without sticky worker reuse: oncall now writes per-thread registry files under `.oncall/registry/threads/`, recording mailbox address, thread id, last processed message id, lease window, execution metadata, and an optional bounded handoff summary from the current backend.
- A first sticky-binding control layer now also exists above that registry: `oncall_supervisor.py` can inspect prior thread bindings, reuse a prior `worker_id` when a backend explicitly allows it, replace stale `running` bindings when the prior lease is expired, and persist those binding decisions plus recovery reasons back into the thread registry. The current `codex-cli` backend explicitly reports `supports_worker_reuse = false`, so it still launches one fresh worker per claimed delivery while remaining visible and future-upgrade-ready.
- `mailbox_oncall.py` now also exposes an initial operational inspection surface via `--inspect-registry`, and the CLI now has an explicit `--backend codex-cli` selector so future backends can land without changing the default invocation shape again.
- A dedicated watch-first entrypoint now also exists in `mailbox_oncall_server.py`: it reuses the same oncall runner as `mailbox_oncall.py`, defaults to `--watch`, and can translate `--idle-exit-after-seconds` into a bounded empty-poll threshold so a local oncall watcher can either stay up or exit cleanly after a configurable idle window.
- That watch-first path now also has a simple PowerShell launcher in `launch_dogfood_oncall_server.ps1`, and the README now documents `--inspect-registry`, `mailbox_oncall_server.py`, and the new launcher for repo-local operator oncall usage.
- The oncall Codex backend can now target an explicit temporary workspace plus temporary `CODEX_HOME` through `--codex-workspace-dir` / `--codex-home-dir` and the matching PowerShell launcher flags, so isolated smoke runs can stage `.tmp_dogfood_live`, auth bootstrap files, and sandbox memories entirely outside the canonical repo.
- A real isolated oncall smoke now also validated that path on a temporary local server: the first run exposed that the static dogfood prompts still hardcoded `E:\agent_misc\mail4agent`, so the prompts were generalized to "current workspace" and `launch_dogfood_oncall_agent.ps1` now prepends an explicit workspace-root instruction; the follow-up run replied from the temporary workspace, recorded that temp workspace in the mailbox reply notes, left retry queue empty, and persisted `handler_cwd`, `workspace_dir`, and `codex_home_dir` into the oncall registry.
- The first experimental app-server oncall backend now also exists in `oncall_exec_app_server.py` and is wired through `mailbox_oncall.py` / `launch_dogfood_oncall_server.ps1` via `--backend app-server`; it starts `codex app-server --listen stdio://`, drives the minimal `initialize -> thread/start -> turn/start -> turn/completed` JSON-RPC flow over stdio, logs returned `app_server_thread_id` plus `app_server_turn_id`, and keeps mailbox plus registry state as the durable source of truth even as process-local thread reuse begins to land.
- That app-server backend is now covered by focused repo-local tests in `test\test_oncall_exec_app_server.py`, including notification-before-response buffering, turn-failure propagation, and transport-error demotion to retryable non-zero exit results instead of crashing the whole supervisor.
- A real isolated app-server smoke now also passed on a temporary git worktree plus temporary runtime and `CODEX_HOME`: the first live run exposed that the executor treated one-second stdio poll timeouts as fatal transport errors, the adapter was hardened to keep polling until the overall request/turn timeout expires, and the retry-path rerun then completed a real claim/reply/ack cycle with `app-server` metadata, a mailbox reply that echoed the temporary workspace path, and an empty final retry queue.
- The app-server oncall backend now also supports sticky per-thread reuse inside one long-lived oncall process: `oncall_supervisor.py` passes thread-assignment context into executors, probes prior `worker_id` liveness before reuse, records `previous_worker_not_available` recovery when a registry binding is no longer live, and calls backend `close()` on watcher shutdown so reusable workers do not linger.
- `oncall_exec_app_server.py` now keeps an in-process pool of live app-server sessions keyed by returned `thread_id`, reuses a live thread for follow-up deliveries on the same mailbox thread, and still falls back to fresh-worker recovery on transport/protocol failure so mailbox plus registry metadata remain the durable truth across restarts.
- A second real isolated app-server smoke now also validated that sticky reuse path on a temporary worktree plus temporary runtime and `CODEX_HOME`: one watch process handled two queued operator deliveries on the same mailbox thread, the second delivery reused the same `app_server_thread_id`, the temporary workspace file captured distinct step-1 and step-2 `message_id` values, and the final retry queue stayed empty.
- Reusable oncall backends now also have bounded worker lifecycle policy controls: `mailbox_worker.py` exposes idle-poll housekeeping ticks, `oncall_supervisor.py` forwards them to the active backend, and `mailbox_oncall.py` / `launch_dogfood_oncall_server.ps1` now expose `--worker-idle-timeout-seconds` plus `--worker-max-age-seconds` so long-running watchers do not keep idle app-server threads forever.
- `oncall_exec_app_server.py` now prunes dead, idle-expired, and max-age-expired workers from the in-process pool, retires over-age workers after a completed turn, and records the active lifecycle policy in execution metadata for registry inspection.
- A third real isolated app-server smoke now also validated idle eviction on a temporary worktree plus temporary runtime and `CODEX_HOME`: one watcher processed the first operator delivery, sat idle past a `2s` worker idle timeout, then processed a second delivery on the same mailbox thread with a new `worker_id`, `recovery_reason = previous_worker_not_available`, two acked deliveries, and an empty final retry queue.
- Cold-recovery app-server runs now also consume bounded continuity metadata from the thread registry: when a fresh worker is created for an existing mailbox thread, `oncall_exec_app_server.py` injects the previous `worker_id`, previous `last_processed_message_id`, `recovery_reason`, and the prior bounded `handoff_summary` into the new prompt context, and it records `recovered_*` metadata back into the execution result for later inspection.
- A fourth real isolated app-server smoke now also validated that recovery-summary path on a temporary worktree plus temporary runtime and `CODEX_HOME`: after idle eviction forced a fresh worker on the same mailbox thread, the final oncall result and persisted thread registry both recorded `recovered_handoff_summary_used = true` together with the previous `worker_id` and previous `last_processed_message_id`.
- The app-server oncall path now also supports per-thread-per-workspace binding within one long-lived watcher: `oncall_supervisor.py` resolves a workspace for each claimed delivery, thread registry files preserve `bindings_by_workspace`, and `oncall_exec_app_server.py` can keep separate live workers for the same mailbox thread across different child workspaces while still reusing the repo-root mailbox CLI and prompt assets.
- A fifth real isolated app-server smoke now also validated cross-workspace sticky reuse on the same mailbox thread: three queued operator deliveries targeted `workspace-a`, then `workspace-b`, then `workspace-a` again, the watcher acked all three, registry state kept two workspace bindings, and the third delivery reused the original `workspace-a` worker instead of the `workspace-b` one.
- A mailbox-language direction note now also exists at `docs\mailbox-language-ir-first-plan-20260328.md`; it evaluates `mailbox_language_spec_v0_2.md` against the current runtime, concludes that the repo is already strong on mailbox transport but missing protocol/state/checker layers, and recommends keeping the mailbox server DSL-agnostic while implementing the spec IR-first plus a separate native-stdio interpreter.
- The first mailbox-language runtime foundation is now also implemented without adding any server-side DSL parsing: `sqlite_mailbox.py` now persists registered protocols, mailbox `accepts/default` bindings, per-thread protocol/state/parent metadata, and thread-handoff relations; `sqlite_mailbox_http.py` now exposes admin routes for managing those runtime artifacts; and `GET /thread` / `GET /admin/thread` now surface the stored runtime metadata so later typed IR work can build on first-class state instead of generic `message_type` strings alone.
- The next mailbox-language slice is now also implemented on top of that foundation: `sqlite_mailbox.py` now executes normalized message envelopes and handoff events through protocol-schema-driven runtime checks, `sqlite_mailbox_http.py` exposes admin routes for `execute_message_envelope` and `execute_handoff_event`, and the new runtime surfaces return structured `error_code` values such as `E_MAILBOX_PROTOCOL_NOT_ACCEPTED`, `E_THREAD_PROTOCOL_MISMATCH`, `E_STATE_TRANSITION_INVALID`, and `E_PAYLOAD_SCHEMA_INVALID`.
- The next mailbox-language usability slice is now also implemented above that runtime: `codex_mailbox_client.py` now exposes protocol registry plus `typed_send` / `typed_spawn` / `typed_handoff` helpers, `client.py` now exposes matching typed commands without requiring raw envelope JSON, and the CLI now prefers `MAILBOX_ADMIN_TOKEN` for those admin-backed typed commands while still honoring explicit token flags.
- The mailbox-language runtime rules are now also shared in a dedicated `mailbox_language_runtime.py` module, and both the server path plus focused unit tests now use that shared protocol normalization / schema validation / payload validation / transition resolution layer instead of keeping those rules buried only inside `sqlite_mailbox.py`.
- The next mailbox-language groundwork slice now also exists for the future stdio interpreter: `mailbox_language_runtime.py` can compile normalized protocol schemas into reusable compiled artifacts, `mailbox_language_cache.py` can persist those artifacts into an explicit local disk cache, and focused tests now cover cache hit/miss plus compiled-artifact validation behavior without changing the server's runtime source of truth.
- The first mailbox-language native-stdio shell is now also implemented in `mailbox_language_stdio.py`: it reads JSON lines requests from stdin, writes one structured JSON response per line, supports `check` / `lower` / `run` for `protocol_schema`, `mailbox_binding`, `message_envelope`, `handoff_event`, and the new `dsl_program` source artifact, reuses `mailbox_language_cache.py` for local protocol compile caching, and drives the same typed admin-backed runtime helpers exposed through `codex_mailbox_client.py` instead of adding DSL parsing to the mailbox server.
- A first source-side parser/checker/lowering slice now also exists in `mailbox_language_source.py`: it parses a bounded `mailbox_language_spec` subset (`protocol`, `mailbox`, `send`, `send text`, `spawn`, `handoff`), resolves mailbox name and input-variable maps supplied by the stdio request, tracks thread protocol/state through sequential source statements, and lowers that source program into the same typed runtime artifacts already used by the JSON-first interpreter shell.
- That source-side checker now also preserves declared field types in the lowered protocol schema and rejects a first bounded class of payload type errors locally during `check` / `lower`: primitive mismatches such as `String` vs `123`, list-shape mismatches such as `[OrderItem]` vs `"sku-1"`, and the same checks for builtin `PlainText/v1` payloads now all return structured `E_PAYLOAD_SCHEMA_INVALID` diagnostics before the request reaches the live mailbox runtime.
- The same bounded DSL layer now also supports non-general-purpose `let` bindings for values and thread aliases: `mailbox_language_source.py` now honors `let name: Type = ...` annotations, checks value bindings against simple scalar/list types, checks thread-producing expressions against `thread<Protocol/version>`, and emits thread-alias metadata so `mailbox_language_stdio.py run` can resolve `let t2 = t1; send to t2 ...` through the same runtime thread id.
- That value layer now also supports bounded nested object literals inside `let` bindings and payload fields. `mailbox_language_source.py` now parses `{ field: expr; ... }` as structured value data, keeps internal variable references distinct from user payload objects, and `mailbox_language_stdio.py run` now preserves those nested objects end to end in the actual runtime message payload.
- The bounded DSL layer now also emits first-class source diagnostics for tool consumers: `MailboxRuntimeError` now has optional `details`, `mailbox_language_source.py` annotates parse/check failures with `source_phase` plus line/column metadata, payload field type mismatches are anchored to the offending source field when possible, and `mailbox_language_stdio.py` forwards those diagnostics as structured JSON fields instead of only returning one free-form error string.

## Completed

- Cloned the repo from GitHub into `E:\agent_misc\mail4agent`.
- Added the lightweight collaboration scaffold: `AGENTS.md`, `.where-agent-progress.md`, and `docs\mainline-plan.md`.
- Verified the repo has no `AGENTS.md` conflicts and kept existing project files untouched.
- Mapped the current protocol, worker, adapter, and demo-agent surface and turned it into a staged Rust-port benchmark scenario list.
- Mapped the existing mailbox product surface into a staged feature-extension requirement matrix for future non-migration benchmarks.
- Reflected the language split into `docs\mainline-plan.md` and added an actionable language backlog at `E:\agent_misc\docs\mail4agent-language-backlog-20260324.md`.
- Chose the dogfood baseline strategy: use the canonical Python repo as the root baseline, then selectively promote benchmark-proven Python feature slices into it rather than adopting a benchmark seed or a single result workspace wholesale.
- Merged the Wave 1 Python dogfood features into the canonical repo runtime and CLI:
  - admin token can now call normal mailbox routes directly across harnesses
  - retry-pending deliveries are now visible through `GET /retry-queue` and `client.py retry-queue`
  - mailbox-scoped `thread-summaries` and `mark-thread-read` now track compact unread state
- Updated `README.md` and `docs\mainline-plan.md` to reflect the new operator and triage surfaces.
- Added a minimal repo-local `test\` package with end-to-end standard-library validation for the merged Wave 1 features.
- Validated the merged baseline with `python -m unittest test.test_wave1_dogfood_features -v` and `python -m unittest discover -v`.
- Added a bounded dogfood bootstrap helper that scripts local harness/project/routing setup and emits runtime-only planner/reviewer mailbox profiles under `.tmp_dogfood\`.
- Added a generic PowerShell launcher for one-shot medium Codex planner/reviewer runs against those runtime profiles.
- Verified the dogfood smoke assets end to end against a temporary local server on `127.0.0.1:8791`:
  - `dogfood_smoke_bootstrap.py` created the runtime planner/reviewer profiles successfully
  - operator admin-token send worked against `planner@mail4agent.dogfood`
  - explicit `client.py login --output token --project-id mail4agent --role planner --session dogfood --agent-name dogfood-planner` produced a valid session token from the harness token
  - planner claim/reply/ack succeeded
  - `thread-summaries`, `retry-queue`, and `mark-thread-read` reflected the expected unread-state transition
- Hardened the dogfood launcher after the first smoke:
  - launcher now bootstraps a repo-local `.codex_home_dogfood` so Codex runs stop depending on the broken global state DB
  - bootstrap now writes role-local `from_address` and `inbox_address` into the planner/reviewer config files
- Ran a full medium Codex smoke on `127.0.0.1:8787`:
  - operator sent one triage request to `planner@mail4agent.dogfood`
  - planner claimed it, replied to operator, and sent a short reviewer handoff
  - reviewer claimed the handoff, returned a compact verdict that requested a tighter planner summary
  - planner claimed that reviewer verdict and sent a revision note back on the same thread
  - final admin view showed a 5-message thread and an empty retry queue
- Promoted the maintenance-aligned small feature into the canonical baseline:
  - added explicit in-memory session invalidation in `sqlite_mailbox.py`
  - added HTTP `POST /logout` and `whoami` expiry metadata in `sqlite_mailbox_http.py`
  - added CLI `logout` plus client-side logout support
  - added repo-local validation in `test\test_session_logout_and_expiry_introspection.py`
  - verified with `python -m unittest test.test_session_logout_and_expiry_introspection -v` and `python -m unittest discover -v`
- Promoted the session-scoped thread-default target into the canonical baseline:
  - added `default_inbox_address` selection to agent-session payloads in `sqlite_mailbox.py`, preferring the role mailbox when role and session mailboxes are both claimable
  - exposed `default_inbox_address` through `GET /whoami` in `sqlite_mailbox_http.py`
  - let `thread-summaries` and `mark-thread-read` fall back to the session default inbox on the server side
  - taught `codex_mailbox_client.py` and `client.py` to surface and prefer that default inbox
  - added repo-local validation in `test\test_session_thread_default_inbox.py`
  - verified with `python -m unittest test.test_session_thread_default_inbox -v` and `python -m unittest discover -v`
- Promoted a first minimal inbox read surface into the canonical baseline:
  - added `get_inbox_messages_for_mailbox` to `sqlite_mailbox.py` using the existing mailbox-visible message model
  - exposed `GET /inbox` in `sqlite_mailbox_http.py` with `limit`, optional exact `message_type`, and session default inbox fallback
  - taught `codex_mailbox_client.py` and `client.py inbox` to use that route and the same default inbox inference
  - added repo-local validation in `test\test_session_inbox_listing.py`
  - updated `README.md` with the new inbox route and CLI examples
- Extended the inbox read surface with unread-only filtering:
  - added `unread_only` support to `get_inbox_messages_for_mailbox` in `sqlite_mailbox.py`
  - exposed `GET /inbox?unread_only=true` in `sqlite_mailbox_http.py`
  - taught `codex_mailbox_client.py` and `client.py inbox --unread-only` to request that filter while preserving session default inbox inference
  - extended `test\test_session_inbox_listing.py` to cover unread-only behavior before and after `mark-thread-read`
  - updated `README.md` with the new CLI example
- Extended the inbox read surface with exact thread filtering:
  - added `thread_id` support to `get_inbox_messages_for_mailbox` in `sqlite_mailbox.py`
  - exposed `GET /inbox?thread_id=<THREAD_ID>` in `sqlite_mailbox_http.py`
  - taught `codex_mailbox_client.py` and `client.py inbox --thread-id <THREAD_ID>` to request that filter while preserving session default inbox inference
  - extended `test\test_session_inbox_listing.py` to cover thread-filtered inbox results over both HTTP and CLI
- Ran the first mailbox-native feedback/update drill through the canonical repo:
  - started a fresh local server/runtime under `.tmp_dogfood_update_drill`
  - sent operator kickoff, planner/reviewer traffic, reviewer feedback, and maintenance notices through mailbox
  - backed up the DB, restarted the same server on the same DB, and verified old session tokens no longer worked
  - re-logged planner/reviewer via the stored harness token and continued the same thread successfully
  - verified final retry queue was empty
- Ran the second mailbox-native maintenance drill against the promoted thread-default build:
  - updated `dogfood_feedback_update_drill.py` to add session-token-only checks that do not rely on `MAILBOX_CONFIG`, `MAILBOX_INBOX_ADDRESS`, or `MAILBOX_FROM_ADDRESS`
  - verified pre-restart planner `whoami` and `thread-summaries --limit 10` succeeded using only the session token
  - restarted on the same DB, confirmed old session tokens failed, and re-logged both roles from the stored harness token
  - verified post-restart planner `whoami`, `thread-summaries --limit 10`, and `mark-thread-read --thread-id <THREAD_ID>` all succeeded using only the fresh session token
  - verified the post-mark-read planner summary flipped to `unread: false` and final retry queue remained empty
- Promoted the dogfood launcher from planner/reviewer-only to role-aware:
  - `dogfood_smoke_bootstrap.py` now writes `operator.mailbox_client.json` and `operator.preview.json`
  - `launch_dogfood_agent.ps1` now handles `planner`, `reviewer`, and `operator`
  - the legacy `launch_dogfood_medium_agent.ps1` remains as a compatibility wrapper
  - operator now logs in with `local_part = operator` and `mailbox_type = group`
  - operator defaults to `gpt-5.4 + high`
- Smoke-validated the operator lane end to end:
  - bootstrap produced `operator.mailbox_client.json`
  - explicit `client.py login --local-part operator --mailbox-type group` returned a valid session with `default_inbox_address = operator@mail4agent.dogfood`
  - a real `launch_dogfood_agent.ps1 operator` run processed one mailbox delivery and replied with structured no-op evidence
  - final thread had two messages and retry queue stayed empty
- Added a first reusable oncall layer on top of the generic worker primitive:
  - extracted the generic `claim/heartbeat/ack/nack` loop into `mailbox_worker.py`
  - rewired `client.py consume` to call that shared module
  - added `mailbox_oncall.py` as the separate role-aware supervisor entrypoint
  - added `launch_dogfood_oncall_agent.ps1` plus `docs\dogfood-high-operator-oncall-prompt.txt`
  - staged runtime config and claimed-delivery JSON into repo-local `.tmp_dogfood_live\` so Codex sandboxed runs can read them even when the real runtime dir lives outside the workspace
- Smoke-validated the first operator-only oncall path:
  - started a temporary local server and bootstrap runtime outside the repo
  - sent one bounded no-op operator delivery to `operator@mail4agent.dogfood`
  - ran `python .\mailbox_oncall.py --role operator --runtime-dir <temp runtime>`
  - confirmed the child operator run replied exactly once on the claimed thread
  - confirmed the supervisor acked the delivery and final retry queue stayed empty
- Ran the first live cross-project routing-explain drill:
  - started a temporary local mailbox server on `127.0.0.1:8797`
  - bootstrapped `consumer_app` plus `shared_tools` into `.tmp_dogfood_cross_project_live`
  - logged in as `consumer_app` planner and sent one `integration_request` to `intake@shared_tools.dogfood`
  - ran `python .\mailbox_oncall.py --role operator --runtime-dir .tmp_dogfood_cross_project_live --consumer-id shared-tools-intake-oncall-1`
  - confirmed supplier-side oncall replied once on the same thread with a machine-readable routing explanation
  - sent one consumer-side `integration_acceptance` reply on the same thread
  - confirmed final `message_count = 3`, final retry queue empty, and the temporary server port was released afterward
- Promoted inbox sender filtering into the canonical baseline:
  - added `from_address` support to `get_inbox_messages_for_mailbox` in `sqlite_mailbox.py`
  - exposed `GET /inbox?from_address=<ADDRESS>` in `sqlite_mailbox_http.py`
  - taught `codex_mailbox_client.py` and `client.py inbox --from-address <ADDRESS>` to request that filter while preserving session default inbox inference
  - extended `test\test_session_inbox_listing.py` to cover the new sender filter and keep the existing inbox filter path passing
  - updated `README.md` with the new CLI example
- Ran the second live cross-project bounded-patch drill:
  - started a temporary local mailbox server on `127.0.0.1:8798`
  - bootstrapped `consumer_app` plus `shared_tools` into `.tmp_dogfood_cross_project_round2_live`
  - logged in as `consumer_app` planner and sent one bounded patch request to `intake@shared_tools.dogfood`
  - ran `python .\mailbox_oncall.py --role operator --runtime-dir .tmp_dogfood_cross_project_round2_live --consumer-id shared-tools-intake-oncall-2`
  - confirmed supplier-side oncall implemented the requested inbox sender filter and replied once on the same thread
  - reran `python -m unittest test.test_session_inbox_listing -v` and `python -m unittest discover -v` locally
  - sent one consumer-side `integration_acceptance` reply on the same thread
  - confirmed final `message_count = 3`, final retry queue empty, and the temporary server port was released afterward
- Added consumer-side mailbox handoff:
  - added `client.py handoff` plus `MailboxHTTPClient.handoff_message(...)`
  - handoff reuses `send` on the same `thread_id`, references the source message via `in_reply_to_message_id`, and embeds the source message snapshot in a `kind = mailbox_handoff` payload
  - added repo-local validation in `test\test_session_handoff.py`
  - verified that `integrator` can read the handoff payload while `planner` still keeps full thread continuity on the original mailbox thread
- Ran the third live cross-project handoff drill:
  - started a temporary local mailbox server on `127.0.0.1:8802`
  - bootstrapped `consumer_app` plus `shared_tools` into `.tmp_dogfood_cross_project_round3_handoff_live`
  - logged in planner and integrator sessions under `consumer_app`
  - sent one `integration_request` to `intake@shared_tools.dogfood`
  - let supplier-side operator oncall reply on the same thread
  - used `client.py handoff` to forward that supplier reply into `integrator@consumer_app.dogfood`
  - had integrator send `integration_acceptance` back to `intake@shared_tools.dogfood` on the same `thread_id`
  - confirmed final retry queue empty and recorded the drill in `docs\dogfood-cross-project-handoff-report-20260326.md`
- Refactored the first oncall implementation along the new separation boundary:
  - added `oncall_registry.py` to persist inspectable run summary and registry JSON files under `.oncall\`
  - added `oncall_supervisor.py` as the reusable watch/claim/result-bookkeeping layer above `mailbox_worker.py`
  - reduced `mailbox_oncall.py` to argument parsing, runtime/config loading, and backend wiring for claimed-delivery execution
  - added `test\test_oncall_supervisor.py` to cover registry path resolution, successful supervisor runs, and failure metadata capture
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_supervisor -v` and `python -m unittest discover -v`
- Added the first explicit execution-backend seam for oncall:
  - added `oncall_exec_codex.py` as the current Codex CLI execution adapter, including launcher command/env assembly and subprocess execution
  - changed `oncall_supervisor.py` to accept structured `ClaimedDeliveryExecutionResult` payloads so future backends can attach bounded execution metadata without teaching the supervisor launcher specifics
  - simplified `mailbox_oncall.py` further so it now builds one executor and passes that adapter into the supervisor instead of assembling `run_subprocess_handler(...)` inline
  - added `test\test_oncall_exec_codex.py` to cover Codex adapter metadata and environment wiring
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_supervisor test.test_oncall_exec_codex -v` and `python -m unittest discover -v`
- Added thread-level supervision metadata without sticky worker reuse:
  - extended `mailbox_worker.py` with an optional per-delivery completion callback so long-running watch loops can persist thread state after every ack or retry decision instead of only at final process exit
  - extended `oncall_supervisor.py` to capture `last_processed_message_id`, claim timestamps, and lease windows, and to write thread registry entries both after successful ack/nack outcomes and after exception paths
  - extended `oncall_registry.py` with `.oncall\registry\threads\` paths plus inspectable thread-state JSON payloads keyed by mailbox address and thread id
  - extended `oncall_exec_codex.py` to surface backend-produced `last_message_path` metadata and a bounded `handoff_summary` from the most recent Codex output file
  - extended `test\test_mailbox_worker.py`, `test\test_oncall_supervisor.py`, and `test\test_oncall_exec_codex.py` to cover the new callback, thread registry, and handoff-summary paths
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_supervisor test.test_oncall_exec_codex -v` and `python -m unittest discover -v`
- Added capability-aware thread binding and recovery scaffolding:
  - extended `oncall_registry.py` with thread-state loading, listing, and `inspect_state()` so role-local registry plus thread bindings can be inspected directly from the CLI
  - extended `oncall_supervisor.py` to derive `worker_kind`, `worker_id`, `supports_worker_reuse`, `reused_worker`, and `recovery_reason` from backend metadata plus existing thread bindings before each delivery runs
  - added stale-binding replacement logic: a prior `running` binding with an expired `lease_until` is now treated as recoverable stale state and replaced with a fresh worker assignment
  - added reusable-binding logic for backends that explicitly opt in; repo-local tests now cover both reuse and stale-replacement paths even though the current Codex CLI backend remains one-shot
  - extended `oncall_exec_codex.py` so the current backend advertises `backend_name = codex-cli`, `worker_kind = codex-cli-run`, and `supports_worker_reuse = false`
  - extended `mailbox_oncall.py` with `--backend codex-cli` and `--inspect-registry`
  - extended `test\test_oncall_supervisor.py` to cover registry inspection, worker reuse, and stale-running binding replacement
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_supervisor test.test_oncall_exec_codex -v` and `python -m unittest discover -v`
- Added a process-facing watch entrypoint and bounded idle-stop control:
  - extended `mailbox_worker.py` with optional `max_empty_polls` handling so watcher-style loops can stop after a configurable number of consecutive empty claims
  - refactored `mailbox_oncall.py` to expose reusable `run_oncall(...)` and `build_parser()` helpers instead of keeping all startup flow trapped inside `main()`
  - added `mailbox_oncall_server.py` as a watch-first entrypoint with `--idle-exit-after-seconds`
  - added `test\test_oncall_server.py` and extended `test\test_mailbox_worker.py` to cover idle-threshold calculation and empty-poll shutdown
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_supervisor test.test_oncall_exec_codex test.test_oncall_server -v` and `python -m unittest discover -v`
- Added a repo-local operator oncall server launcher and documented the new operational surface:
  - added `launch_dogfood_oncall_server.ps1` to run `mailbox_oncall_server.py` with role, runtime dir, backend, optional consumer id, and optional idle-exit control
  - updated `README.md` with examples for `--inspect-registry`, `mailbox_oncall_server.py`, and the new PowerShell launcher
- Added process-local sticky reuse for the app-server backend:
  - changed `oncall_supervisor.py` so executors now receive a `ClaimedDeliveryExecutionContext` with delivery plus thread-assignment metadata and an optional backend liveness probe
  - added app-server worker pooling, `can_reuse_worker(...)`, and explicit `close()` cleanup in `oncall_exec_app_server.py` / `mailbox_oncall.py`
  - extended `test\\test_oncall_supervisor.py`, `test\\test_oncall_exec_app_server.py`, and `test\\test_oncall_exec_codex.py` to cover the new context contract, missing-worker recovery, and same-thread reuse
  - verified with `python -m unittest test.test_oncall_exec_codex test.test_oncall_exec_app_server test.test_oncall_supervisor -v`, `python -m unittest discover -v`, and a real two-delivery isolated smoke against `--backend app-server`
- Added lifecycle pruning for reusable app-server workers:
  - extended `mailbox_worker.py` and `oncall_supervisor.py` with idle-tick callbacks so watchers can run backend housekeeping even while no deliveries are claimable
  - extended `mailbox_oncall.py` and `launch_dogfood_oncall_server.ps1` with `--worker-idle-timeout-seconds` and `--worker-max-age-seconds` for reusable backends, with `0` disabling a limit
  - extended `oncall_exec_app_server.py` so worker pools now prune dead workers, idle-expired workers, and max-age-expired workers, while still preserving process-local sticky reuse when policy allows it
  - extended `test\\test_mailbox_worker.py` and `test\\test_oncall_exec_app_server.py` to cover idle housekeeping, idle-timeout pruning, and max-age-driven worker rotation
  - verified with `python -m unittest test.test_mailbox_worker test.test_oncall_exec_app_server test.test_oncall_supervisor test.test_oncall_exec_codex test.test_oncall_server -v`, `python -m unittest discover -v`, and a real idle-eviction smoke against `--backend app-server --worker-idle-timeout-seconds 2 --worker-max-age-seconds 0`
- Added registry-backed cold-recovery context for fresh app-server workers:
  - extended `oncall_exec_app_server.py` so a new worker can receive prior `handoff_summary`, previous `worker_id`, previous `last_processed_message_id`, and `recovery_reason` in its prompt context when reuse is not possible
  - extended `oncall_registry.py` and `test\\test_oncall_supervisor.py` so `recovered_handoff_summary_used`, `recovered_recovery_reason`, `recovered_last_processed_message_id`, and `recovered_previous_worker_id` persist into thread registry files
  - extended `test\\test_oncall_exec_app_server.py` to cover recovery-summary prompt injection on cold restart and explicit non-injection when a live worker is reused
  - verified with `python -m unittest test.test_oncall_exec_app_server test.test_oncall_supervisor -v`, `python -m unittest test.test_mailbox_worker test.test_oncall_exec_app_server test.test_oncall_supervisor test.test_oncall_exec_codex test.test_oncall_server -v`, `python -m unittest discover -v`, and a real recovery-summary smoke against `--backend app-server --worker-idle-timeout-seconds 2 --worker-max-age-seconds 0`

## In Progress

- Deciding whether the first operator oncall path should remain operator-only for one more iteration or whether planner/reviewer should get equivalent claimed-delivery prompts too.
- Deciding whether the consumer side of the cross-project drill should remain human-driven CLI or grow into a planner/integrator oncall path next.
- Deciding whether the next cross-project iteration should merely use the new planner-to-integrator handoff, or go further and add an `integrator` oncall lane on top of it.
- Deciding whether planner should receive an explicit review receipt after integrator acceptance so review results flow back into the original planner mailbox without widening thread visibility.

## Next

- Decide whether direct mailbox access on normal routes should stay static-admin-token-only or also accept admin-account credentials.
- Choose the next promoted Python feature wave after the current Wave 1 baseline.
- Keep future validation additions bounded and repo-local rather than importing benchmark harness scaffolding wholesale.
- Keep future product-surface work Python-first and use the new language backlog when selecting Rust follow-up ports.
- Decide whether the planner/reviewer medium smoke and operator high-effort update lane should stay one-shot and human-launched or grow into reusable long-running mailbox workers.
- Decide whether planner/reviewer should get their own role-aware oncall prompts after the operator-only path proves stable.
- Decide whether mailbox-native oncall needs a stronger global or cross-route lock beyond the new mailbox-thread claim serialization before we support more aggressive multi-supervisor deployment.
- Decide whether the next cross-project drill should stay text-first (`routing_explain` follow-up) or move to a bounded code-changing supplier request.
- Decide whether planner and integrator should get their own dedicated cross-project runtime prompts next, or whether the consumer side should stay mostly manual for one more iteration.
- Use `client.py handoff` as the default consumer-side review bridge for future cross-project drills before changing any server-side visibility rules.
- Decide whether cross-project review completion should stay mailbox-scoped or gain an explicit planner-facing receipt / handback message after integrator acceptance.
- Decide whether the next oncall iteration should stay stateless-plus-serialized, or introduce thread-sticky agents via a separate oncall server without turning app-server state into the only durable workflow record.
- Choose the first real bounded code-changing mailbox task for the new operator lane now that the bootstrap/login/launcher smoke has passed.
- Choose the next IR-first `mailbox_language_spec` slice after the first bounded source-DSL lowering layer: broader grammar coverage, stronger declaration/type diagnostics, and better source spans on top of the current `dsl_program` interpreter path.

## Detailed Docs

- `E:\agent_misc\mail4agent\docs\mainline-plan.md`
- `E:\agent_misc\mail4agent\docs\rust-port-test-scenarios-20260322.md`
- `E:\agent_misc\mail4agent\docs\feature-extension-test-requirements-20260322.md`
- `E:\agent_misc\mail4agent\docs\dogfood-operator-update-flow-20260324.md`
- `E:\agent_misc\mail4agent\docs\mailbox-oncall-separation-20260328.md`
- `E:\agent_misc\mail4agent\docs\mailbox-oncall-implementation-plan-20260328.md`
- `E:\agent_misc\mail4agent\docs\mailbox-language-ir-first-plan-20260328.md`
- `E:\agent_misc\docs\mail4agent-python-rust-split-plan-20260324.md`
- `E:\agent_misc\docs\mail4agent-language-backlog-20260324.md`
