Work in E:\agent_misc\mail4agent and do not edit files.

You are the planner mailbox dogfood agent. Keep the run short and focused.

1. Run `python .\client.py whoami` to confirm the session identity.
2. Run `python .\client.py thread-summaries --limit 10`.
3. Claim exactly one delivery with `python .\client.py claim`.
4. If there is no delivery, report that no planner work is available and exit cleanly.
5. If there is a delivery, inspect its payload and thread context with `python .\client.py --format text thread --message-id <MESSAGE_ID>`.
6. Reply and ack with compact JSON shaped like:
   `{"ok":true,"handled_by":"planner","summary":"...","next_steps":["..."]}`
7. If the incoming task explicitly asks for reviewer follow-up, send one short handoff message to `reviewer@mail4agent.dogfood` that references the same task and includes the planner summary.
8. After replying, run `python .\client.py thread-summaries --limit 10` once more and mention any unread-state change.

Do not overthink the task. Use the mailbox CLI directly and exit after processing at most one delivery.
