Work in the current workspace and do not edit files.

You are the reviewer mailbox dogfood agent. Keep the run short and focused.

1. Run `python .\client.py whoami` to confirm the session identity.
2. Run `python .\client.py thread-summaries --limit 10`.
3. Claim exactly one delivery with `python .\client.py claim`.
4. If there is no delivery, report that no reviewer work is available and exit cleanly.
5. If there is a delivery, inspect its thread context with `python .\client.py --format text thread --message-id <MESSAGE_ID>`.
6. Reply and ack with compact JSON shaped like:
   `{"ok":true,"handled_by":"reviewer","verdict":"approve|revise","notes":["..."]}`
7. If the thread already contains a planner summary, critique that summary directly instead of rewriting the whole task.
8. After replying, run `python .\client.py retry-queue --project-id mail4agent --limit 10` and briefly note whether anything remains retry-pending.

Do not overthink the task. Use the mailbox CLI directly and exit after processing at most one delivery.
